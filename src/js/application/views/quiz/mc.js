define([
    'jquery',
    'backbone',
    './base'],
    function ($, Backbone, BaseQuizView) {
        "use strict";
        var McQuizView = BaseQuizView.extend({
            // ...
        });
        return McQuizView;
    });