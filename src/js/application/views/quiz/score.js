define([
    'jquery',
    'backbone',
    './base'],
    function ($, Backbone, BaseQuizView) {
        "use strict";
        var ScoreQuizView = BaseQuizView.extend({
            // ...
        });
        return ScoreQuizView;
    });