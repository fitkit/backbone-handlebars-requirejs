define([
    'jquery',
    'backbone',
    './base'],
    function ($, Backbone, BaseQuizView) {
        "use strict";
        var PersonalityQuizView = BaseQuizView.extend({
            // ...
        });
        return PersonalityQuizView;
    });