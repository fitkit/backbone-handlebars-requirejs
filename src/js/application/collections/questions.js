define([
    'jquery',
    'backbone'],
    function ($, Backbone) {
        "use strict";
        var QuestionCollection = Backbone.Collection.extend({
        });
        return QuestionCollection;
    });

