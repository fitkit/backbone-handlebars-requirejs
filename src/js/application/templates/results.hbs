<div class="centered-container">
	<h2 class="results-subtitle">{{ title }}</h2>
	{{#if img}}
	<div class="results-img" style="background-image:url({{ img }});"></div>
	{{/if}}
	<div class="results-text">{{{ description }}}</div>
</div>